const BASE_PATH = import.meta.env.VITE_BASE_PATH || "/donation-board/";
const BACKEND_SERVER_ROOT = import.meta.env.VITE_BACKEND_SERVER_ROOT || "http://localhost:8080";

export default {
	BASE_PATH,
	BACKEND_SERVER_ROOT,
};
